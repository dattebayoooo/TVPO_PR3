name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Python version
        run: |
          python3 --version
          pip3 --version

      - name: Install dependencies
        run: |
          pip3 install -r requirements.txt

      - name: Run tests
        run: |
          python3 -m pytest tests/ -v --tb=short

  deploy:
    name: Deploy
    needs: test
    if: success()
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to production server
        run: |
          cd /home/kirill/TVPO_PR3
          git pull origin main